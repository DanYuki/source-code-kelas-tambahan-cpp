#include <iostream>

using namespace std;

int main() {
    // Diberikan sebuah array angka sebagai berikut:
    int numbers[50];
    for (int i = 0; i < 50; i++)
    {
        numbers[i] = i+1;
    }

    // Menggunakan logic fizzbuzz, loop element array and print output berdasarkan masing-masing
    // angka di dalam array tersebut.
    
    
}